# PathPilot Product Roadmap

This file is the execution tracker for the two approved 50-item roadmaps. Status values are `DONE`, `IN_PROGRESS`, `BLOCKED`, and `TODO`.

## Execution rule

- Fix broken CI/deploy first.
- Work in safe batches and verify each batch before the next one.
- If a task needs an external account action, credential, paid service, domain, store approval, or other user-only step, mark it `BLOCKED` with the exact requirement and continue with another unblocked task.
- Never weaken authentication, email verification, owner protections, or persistence to make a task pass.
- Preserve the Railway SQLite volume and production data.

## Progress notes

- 2026-08-30: Round-five conversational quality regression coverage added. New deterministic tests cover noisy Arabic/Egyptian/Arabizi social turns, repeated-letter and punctuation normalization, short contextual follow-ups that must remain on the reasoning path when prior context exists, mixed-language social intent recognition, and protection against swallowing substantive explain/debug/email requests. A33 remains `DONE` with ongoing regression hardening.
- 2026-08-30: A22/A33 context follow-up resolution hardened. Elliptical references now recognize short Arabic, English, mixed-language, and common Arabizi forms such as `دي كمان`, `what about the second one`, `w da`, `nafs elly fat`, and `kml`, while standalone requests such as `ليه السماء زرقا` / `why is DNS cached` no longer get pulled into an unrelated prior turn merely because they start with “why”. Full conversation-context metadata now gives the current strong self-referential Arabic grammatical signal priority over older signals, while ambiguous current wording falls back to the newest prior strong signal. No name/photo/voice/device inference or demographic profile is introduced. Regression coverage locks follow-up continuity, mixed-language reference resolution, new-topic isolation, and female/male/current-over-prior behavior. A22/A33 remain `DONE` with ongoing quality hardening.
- 2026-08-30: A33 Egyptian Arabizi semantic normalization expanded beyond fixed greeting cues. Common state/help spellings such as `msh fahm`, `msh sh8al`, `mhtag msa3da`, `3ayza msa3da`, and `ana kwayes` now normalize into the existing Arabic intent/context pipeline, preserving Arabic response mode, context-sensitive confusion/frustration fallthrough, and substantive `explain/debug` routing. The normalizer maps reusable token families instead of adding separate phrase templates; regression tests cover normalization, mixed tech wording, language selection, action-bearing fallthrough, and prior-context behavior. A33 remains `DONE` with continued conversational hardening.
- 2026-08-30: A22/A33 conversational grammar quality hardened after the first privacy-safe gender-aware release. Strong first-person Egyptian evidence now covers common forms such as `عايز/عايزة`, `كويس/كويسة`, `قلقان/قلقانة`, `مضغوط/مضغوطة`, `متضايق/متضايقة`, `مرهق/مرهقة`, and `مستعد/مستعدة`, plus explicit Arabic `ذكر/أنثى`. Feminine casual-address gaps such as `بيكي` and `تحبي` are covered, while unknown/ambiguous users now receive genuinely neutral Egyptian wording that avoids unnecessary masculine forms such as `بيك`, `تحب`, `معاك`, `وقت ما تحب`, and masculine imperatives. Names and third-person wording remain excluded, newer strong self-reference still overrides older context, and no demographic profile is persisted. Regression tests cover explicit/self-referential detection, third-person false positives, context override, feminine/masculine preservation, and deterministic neutral fallback. A22/A33 remain `DONE` with ongoing conversational-quality hardening.
- 2026-08-30: A22/A33 conversational context advanced with privacy-scoped Arabic grammatical-form adaptation. The chat now derives only strong self-referential masculine/feminine evidence from the current local conversation (for example explicit `انا بنت` / `انا ولد` or clear first-person forms such as `انا محتاجة` / `انا محتاج`), carries a compact grammatical-address hint across otherwise unrelated turns in the same chat without injecting the old turn content, lets newer evidence override older context, and never infers from names, photos, voice, devices, stereotypes, or second-person guesses. The lightweight conversational fast path consumes the same context envelope, uses feminine/masculine wording when supported, neutralizes obvious gendered wording when ambiguous, and preserves contextual confusion/frustration follow-ups for the full reasoning path. No demographic field or server-side profile is stored. Regression tests cover explicit/self-referential detection, ambiguity, latest-overrides-prior behavior, context carryover without semantic leakage, feminine/masculine/neutral responses, English isolation, and follow-up fallthrough.
- 2026-08-30: A33 conversational understanding expanded into common Egyptian Arabizi/Romanized Arabic and reciprocal social turns without turning the fast path into an exact-phrase dictionary. `conversation-intent.js` now recognizes cues such as `ezayak`, `3amel eh`, `shokran`, `tmam`, `yalla`, and `ma3lesh`, keeps those turns in Arabic response mode, handles reciprocal checks such as `وانت؟` / `and you?`, presence checks, and additional casual transitions, while broader action-bearing guards keep requests such as drafting email, reviewing code, explaining, or debugging on the normal reasoning/context pipeline. Regression tests cover normalization, Arabizi language selection, mixed-language fillers, reciprocal turns, presence/identity variants, and action fallthrough. A33 remains `DONE` as a shipped capability with continuing conversational-quality expansion.
- 2026-08-30: A33 conversational diversity advanced with language-scoped cross-intent wording freshness and stronger semantic short-turn matching. The variation engine now keeps a bounded history of response-catalog signatures in addition to per-intent variant IDs, allowing adjacent social intents to avoid near-duplicate phrasing such as repeatedly ending with the same “هات اللي بعده ونكمل” wording when a fresh alternative exists. No user prompt text is stored. Conversational archetype matching now ignores harmless Egyptian/English social fillers (`يا معلم`, `bro`, `بجد`, `so much`) while retaining action-bearing safeguards, expands repeated confusion/frustration and positive-update recognition, and keeps Arabic-first code switching stable when English product or technical words appear inside an otherwise Arabic turn. Regression tests cover global near-duplicate avoidance, singleton fallbacks, language isolation, filler-aware matching, code switching, repeated-state routing, and scoped-request fallthrough. A33 remains `DONE` as a shipped capability with continuing quality expansion.
- 2026-08-30: A33 conversational naturalness advanced again by separating lightweight intent understanding into `conversation-intent.js` instead of growing a brittle exact-phrase list. The new archetype detector combines normalization, embedded phrase recognition, short-message token matching, mixed Arabic/English handling, and action-bearing safeguards so social wording such as `thanks` or `تمام` cannot swallow a real follow-up like `كمل الشرح` / `explain the second part`. Coverage now includes ready/start transitions, encouragement requests, positive user updates, embedded Egyptian variants such as `عامل ايه يا معلم`, and context-sensitive confusion/frustration fallthrough support while preserving non-repeating variant selection. Regression tests cover mixed-language turns, embedded phrases, scoped-request fallthrough, normalization, context-sensitive routing, and variant rotation. A33 remains `DONE` as a shipped capability with continuing quality expansion.
- 2026-08-30: A33 conversational naturalness advanced with a reusable variation-history engine and broader lightweight intent coverage. Casual Arabic, Egyptian Arabic, and English turns now cover greetings by time of day, status questions, thanks, acknowledgements, farewells, apologies, confusion, generic help, frustration, compliments, laughter, identity, capability, and assistant-status questions without invoking unnecessary provider/RAG/search work. Response pools rotate against a small on-device history of variant IDs, so repeated intent in a new local chat avoids recently used wording without storing prompt content. Normalization now tolerates Arabic diacritics, tatweel, punctuation, common letter variants, and exaggerated repeated characters, while scoped/substantive requests still fall through to the normal reasoning pipeline. Regression tests cover intent routing, normalization, safe storage recovery, and deterministic non-repetition across repeated chats.
